{
  "name": "siemens-capabilities-heroku",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd artifacts/salesforce-capabilities && npm install && npm run build",
    "heroku-postbuild": "npm run build",
    "start": "node artifacts/salesforce-capabilities/server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
